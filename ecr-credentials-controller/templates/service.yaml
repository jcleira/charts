apiVersion: v1
kind: Service
metadata:
  name: {{ include "ecr-credentials-controller.fullname" . }}
  labels:
    {{- include "ecr-credentials-controller.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ecr-credentials-controller.selectorLabels" . | nindent 4 }}
