apiVersion: v1
kind: Service
metadata:
  name: {{ include "crd.fullname" . }}
  labels:
    {{- include "crd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "crd.selectorLabels" . | nindent 4 }}
